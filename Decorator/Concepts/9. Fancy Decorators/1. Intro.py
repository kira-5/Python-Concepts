"""
    ! Fancy Decorators:
       
       ? You can also use decorators with classes, these are known as fancy decorators in Python.
       
       * There are two possible ways for doing this:
            ? There are two possible ways for doing this:
            ? Decorating a complete class.

"""
